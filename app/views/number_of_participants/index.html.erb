<%= render partial: "shared/header", locals: {menu: "Number"} %>
<p id="notice"><%= notice %></p>
<div>　</div>
<div>　</div>
<div>　</div>
<table>
    <thead>
	<tr>
	    <th>Number</th>
	    <th colspan="3">Options</th>
	</tr>
    </thead>
    <tbody>
	<% @number_of_participants.each do |number_of_participant| %>
	    <tr>
		<td><%= number_of_participant.number %></td>
		<td>
		    <%= link_to image_tag('show.png', { :border => '0', :alt => 'Show'}), number_of_participant %>
		    <%= link_to 'Show', number_of_participant %>
		</td>
		<td>
		    <%= link_to image_tag('edit.png', { :border => '0', :alt => 'Edit'}), edit_number_of_participant_path(number_of_participant) %>
		    <%= link_to 'Edit', edit_number_of_participant_path(number_of_participant) %>
		</td>
		<td>
		    <%= link_to image_tag('destroy.png', { :border => '0', :alt => 'Destroy'}), number_of_participant, method: :delete, data: { confirm: 'Are you sure?' } %>
		    <%= link_to 'Destroy', number_of_participant, method: :delete, data: { confirm: 'Are you sure?' } %>
		</td>
	    </tr>
	<% end %>
    </tbody>
</table>

<%= render 'shared/footer' %>		
    </body>
</html>  
